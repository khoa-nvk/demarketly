import { readInt, writeInt } from "../helpers.mjs";
import { ArgumentError } from "../../../utils/errors.mjs";
export default {
  serialize(value) {
    if (value < 0) throw new ArgumentError('value', 'greater or equal to 0', value);
    return writeInt(value);
  },

  deserialize(value) {
    return readInt(value);
  }

};
//# sourceMappingURL=u-int.mjs.map