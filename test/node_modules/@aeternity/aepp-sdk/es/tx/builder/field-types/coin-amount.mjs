import uInt from "./u-int.mjs";
import { formatAmount } from "../../../utils/amount-formatter.mjs";
import { ArgumentError } from "../../../utils/errors.mjs";
export default { ...uInt,

  serializeAettos(value) {
    if (value == null) throw new ArgumentError('value', 'provided', value);
    return value;
  },

  serializeOptional(value, _ref) {
    let {
      denomination,
      ...options
    } = _ref;
    return uInt.serialize(this.serializeAettos(value != null ? formatAmount(value, {
      denomination
    }) : value, options));
  },

  serialize(value, options) {
    return this.serializeOptional(value, options);
  }

};
//# sourceMappingURL=coin-amount.mjs.map