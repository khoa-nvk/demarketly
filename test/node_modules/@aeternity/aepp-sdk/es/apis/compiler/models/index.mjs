/** Known values of {@link CompileOptsBackend} that the service accepts. */
export let KnownCompileOptsBackend;
/**
 * Defines values for CompileOptsBackend. \
 * {@link KnownCompileOptsBackend} can be used interchangeably with CompileOptsBackend,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **fate** \
 * **aevm**
 */

(function (KnownCompileOptsBackend) {
  KnownCompileOptsBackend["Fate"] = "fate";
  KnownCompileOptsBackend["Aevm"] = "aevm";
})(KnownCompileOptsBackend || (KnownCompileOptsBackend = {}));

/** Known values of {@link BytecodeCallResultInputBackend} that the service accepts. */
export let KnownBytecodeCallResultInputBackend;
/**
 * Defines values for BytecodeCallResultInputBackend. \
 * {@link KnownBytecodeCallResultInputBackend} can be used interchangeably with BytecodeCallResultInputBackend,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **fate** \
 * **aevm**
 */

(function (KnownBytecodeCallResultInputBackend) {
  KnownBytecodeCallResultInputBackend["Fate"] = "fate";
  KnownBytecodeCallResultInputBackend["Aevm"] = "aevm";
})(KnownBytecodeCallResultInputBackend || (KnownBytecodeCallResultInputBackend = {}));

/** Known values of {@link DecodeCalldataBytecodeBackend} that the service accepts. */
export let KnownDecodeCalldataBytecodeBackend;
/**
 * Defines values for DecodeCalldataBytecodeBackend. \
 * {@link KnownDecodeCalldataBytecodeBackend} can be used interchangeably with DecodeCalldataBytecodeBackend,
 *  this enum contains the known values that the service supports.
 * ### Known values supported by the service
 * **fate** \
 * **aevm**
 */

(function (KnownDecodeCalldataBytecodeBackend) {
  KnownDecodeCalldataBytecodeBackend["Fate"] = "fate";
  KnownDecodeCalldataBytecodeBackend["Aevm"] = "aevm";
})(KnownDecodeCalldataBytecodeBackend || (KnownDecodeCalldataBytecodeBackend = {}));
//# sourceMappingURL=index.mjs.map