import { createMetaTx } from "../contract/ga.mjs";

/**
 * @deprecated Use createMetaTx instead
 * @hidden
 */
export async function signUsingGA(tx, _ref) {
  let {
    authData,
    authFun,
    ...options
  } = _ref;
  return createMetaTx(tx, authData, authFun, options);
}
/**
 * @deprecated Use getHeight instead
 * @hidden
 */

export async function height(_ref2) {
  let {
    onNode
  } = _ref2;
  return (await onNode.getCurrentKeyBlockHeight()).height;
}
//# sourceMappingURL=methods.mjs.map